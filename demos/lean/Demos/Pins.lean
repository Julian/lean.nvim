-- You can pin the goal state at any cursor position
-- to keep it visible while you work elsewhere.

example (p q r : Prop) (hp : p) (hq : q) (hr : r) : p ∧ q ∧ r := by
  refine ⟨?_, ?_, ?_⟩
  · sorry
  · sorry
  · sorry
